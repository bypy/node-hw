// мы будем часть "сырого" ”–Ћа (req.originalUrl) использовать как им€ файла в файловой системе
// но в ”–Ће пробелы, русские буквы и другие символы url-кодируютс€ (из " " получаетс€ "%20", из "п" - "%D0%BF")
// а в именах файлов в файловой системе такое кодирование не примен€етс€
// в браузере декодирование из ”–Ћ-формата в обычную строку делаетс€ вызовом urldecode
// под Node.js - вызовом querystring.unescape
// в req.params (данные из частей ”–Ћа, пока не проходили), req.query (get-данные), req.body (post-данные) это декодирование уже сделано